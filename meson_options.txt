option('profile',
  type: 'combo',
  choices: ['default', 'nightly'],
  value: 'default',
  description: 'Which build this is. A nightly installs beside a release: ' +
               'its own application id, settings, data directory and ' +
               'workspaces. A branch is tried by building it from the ' +
               'nightly itself, which is a nightly like any other.',
)

option('commit',
  type: 'string',
  value: '',
  description: 'The commit this was built from, shown by --version. Empty ' +
               'when built outside a checkout.',
)
